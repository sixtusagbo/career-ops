Two stages.

For tonight's first deploy I'll use it locally on my Mac to apply the manifests and issue the cert. Nothing stored, nothing committed.

For the pipeline's ongoing deploys, yes, a kubeconfig as a protected + masked file-type CI/CD variable. But I wouldn't put your full Rancher admin kubeconfig in CI. I'd create a dedicated ServiceAccount scoped to just the cronedge namespace and use that narrow kubeconfig for the variable, so the CI credential can only roll out the site, nothing cluster-wide. I'll set that up once we're live (or if you'd rather mint that ServiceAccount + kubeconfig yourself, even better, I'll tell you the exact RBAC it needs).

So the Rancher Download KubeConfig is just for me to bootstrap from my Mac now.
